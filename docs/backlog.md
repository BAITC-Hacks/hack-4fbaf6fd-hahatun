# Бэклог «Аким на 5 часов»

Спека: `docs/plan.md`. Оценки в минутах при работе с AI-агентом. Статусы: `todo` / `wip` / `review` / `done`.
Ветки: Амир `feat/engine-consilium`, Мирас `feat/ui`. В `main` только через PR + `/code-review` Амира.

## Статус (обновлять через /handoff)

| Задача | Статус | Где |
|---|---|---|
| A0 скаффолд | done | main `d9e8d20` |
| A1 контракт типов + фикстура | done | main |
| A2 данные, A3 валидатор, A4 движок + факты | done | main, 21 тест |

## Таймлайн (пересобран 14:25, дедлайн 18:00)

Вырезано: лидерборд, тепловая карта, события, экспорт, SQLite (прогоны в JSON `data/runs/`), полный перебор
(только соседи по одной замене). Docker только после README.

| Время | Амир | Мирас (на фикстуре) |
|---|---|---|
| 14:25–14:35 | движок в main, A5-lite соседи | M0 оболочка и тема |
| 14:35–15:15 | A6 LLM-обёртка, A7 эксперты, A8 синтезатор | M1 брифинг, M2 карточка и бюджет |
| 15:15–15:50 | A9 ревизоры, A10 арбитр | M3 кабинет с живым Score |
| 15:50–16:20 | A11 пайплайн, SSE, JSON-хранилище | M4 вердикт |
| 16:20–16:50 | A12 README, merge UI, сквозной прогон | M5 зал заседаний (без diff-подсветки) |
| **16:50** | **сдать черновик на платформе** | |
| 16:50–17:30 | правки, три сида, скриншоты | M6 применить поручение, стык с SSE |
| 17:30–18:00 | финальная сдача, без выходов | буфер |

## Must

| ID | Кто | Задача | Оценка | Зависит | Критерии приёмки | Статус |
|---|---|---|---|---|---|---|
| A0 | оба | Скаффолд: `create-next-app` (TS, App Router, Tailwind), shadcn init, vitest, drizzle + better-sqlite3, `.gitignore` с `.env` и `data/`, `.env.example`, первый коммит в `main` | 15 | — | `yarn dev` открывает пустую страницу, `yarn test` проходит, `.env` игнорируется | done |
| A1 | Амир | `src/lib/types.ts` по разделу 5 спеки + `fixtures/sample-run.json` (полный `Run` руками, правдоподобные тексты) → PR в `main` | 20 | A0 | Мирас может рендерить любой экран из фикстуры без бэка | todo |
| A2 | Амир | `lib/data/*`: 5 районов, 14 мер, веса, синергии, конфликты. Данные 1-в-1 из датасета | 15 | A1 | unit-тест: суммы весов = 1, доли населения = 1 | todo |
| A3 | Амир | `lib/engine/validator.ts`: 8 правил, все ошибки списком, `cost`/`remaining` | 25 | A2 | тест на каждый `ValidationCode`, пример из датасета `ok`, дешёвый набор за 61 `ok` | todo |
| A4 | Амир | `lib/engine/engine.ts` + `facts.ts`: расчёт, вклад мер, критические, факты с ID и scope | 35 | A2 | золотой тест: база 52.56, пример 56.54, nCrit 0; синергия M10+M12 даёт B1 +2 в Нуре | todo |
| A6 | Амир | `lib/consilium/llm.ts`: обёртка над AI SDK, retry 429, fingerprint, трейс usage, режим disabled с фикстурой | 20 | A1 | без ключа возвращает фикстуру и `llmEnabled=false`; с ключом один тестовый вызов проходит | todo |
| A7 | Амир | `experts.ts` + `prompts.ts`: 6 ролей, параллельно, zod-схема, фильтр `factRefs` | 30 | A4, A6 | 6 мнений за < 8 с, все `factRefs` существуют | todo |
| A8 | Амир | `synthesizer.ts`: Draft v1 и пересмотр по списку проваленных условий | 20 | A7 | текст содержит сильные стороны, риски, последствия, рекомендацию | todo |
| A9 | Амир | `reviewers.ts`: C1 и C2 кодом, C3–C6 одним LLM-вызовом, цикл cap 2, все черновики сохраняются | 35 | A8 | тест: число не из фактов проваливает C1; невалидная рекомендация проваливает C2; цикл не идёт дальше 2 кругов | todo |
| A10 | Амир | `arbiter.ts`: таблица исходов в коде + LLM для disputes/justification/mandates, фильтр mandates по переданным улучшениям | 25 | A9, A5 | три исхода воспроизводимы на трёх сидах | todo |
| A11 | Амир | `pipeline.ts` + `api/run` SSE + `db` схема и запись `Run` + `api/runs/[id]` | 30 | A9, A10 | `curl -N` показывает поток событий; прогон открывается по `runId` после перезагрузки | todo |
| A12 | Амир | README по 11 пунктам + диаграмма пайплайна + Dockerfile + `docker-compose.yml` + `.env.example` с комментариями | 40 | A11 | чистый клон → `yarn && cp .env.example .env && yarn dev` работает; `docker compose up` работает | todo |
| A13 | Амир | Финал: 3 сида прогнать, скриншоты в README, `grep sk-` по репо пусто, merge, сдача на платформе | 30 | всё | сдано, ссылка на репо открывается у организаторов | todo |
| M0 | Мирас | Тема и shell: шрифты, палитра, `AppShell` с шапкой «Аким на 5 часов», навигация | 20 | A0 | все три маршрута открываются с общей оболочкой | review |
| M1 | Мирас | Брифинг `/`: плитки районов с цветом по D, профили, базовый Score, бюджет, поле команды, кнопка | 25 | A1 | данные из `lib/data`, красные провалы Нуры видны | review |
| M2 | Мирас | Компоненты карточки меры и панели бюджета (budget bar, счётчик 5/5, лимиты направлений, ошибки) | 25 | A1 | карточка показывает цену, лаг, эффекты, бейджи синергии и конфликта | review |
| M3 | Мирас | Кабинет `/play`: каталог по 5 направлениям, выбор района, живой Score через `engine` на клиенте, блокировка кнопки при ошибках, POST в `/api/run` и переход на результат | 70 | M2, A3, A4 | пример из датасета собирается и показывает 56.54 до отправки; overspend красит полосу | review |
| M4 | Мирас | Вердикт `/result/[runId]`: шапка с числами и бейджами, radar до/после с переключателем района, таблица показателей со стрелками, блок «Как считалось» | 45 | A1 | рендерится из `fixtures/sample-run.json` | review |
| M5 | Мирас | Зал заседаний: стол экспертов со стримом, табло ревизоров, история черновиков с подсветкой отклонённых абзацев, резолюция арбитра с печатью и спорами | 70 | M4 | все события `ConsiliumEvent` из фикстуры отображаются; при обрыве SSE делается GET прогона | review |

## Should

| ID | Кто | Задача | Оценка | Зависит | Критерии приёмки | Статус |
|---|---|---|---|---|---|---|
| A5 | Амир | `optimizer.ts`: перебор, кэш `data/optimum.json`, перцентиль, топ-3 улучшений | 30 | A4 | перебор < 15 с на ноутбуке; каждое улучшение валидно и лучше исходного | todo |
| M6 | Мирас | «Применить поручение»: подстановка улучшения в кабинет и новый прогон | 15 | M5 | после клика кабинет открыт с новым набором | review |
| M7 | Мирас | Экран прогресса стадий во время прогона (построчно, без спиннера) | 20 | M5 | видны стадии validate → persist по мере событий | review |

## Could

| ID | Кто | Задача | Оценка | Зависит | Статус |
|---|---|---|---|---|---|
| M8 | Мирас | Лидерборд `/leaderboard` + `api/runs` | 30 | A11 | review |
| M9 | Мирас | Тепловая карта районов (стилизованная схема Астаны, не гео) | 30 | M1 | todo |
| A14 | Амир | Карточка «внезапное событие»: шок по одному показателю, перераспределить одну меру | 40 | A11 | todo |
| A15 | Амир | Экспорт питча команды в markdown из `Run` | 20 | A11 | todo |

## Won't

Авторизация (в том числе ЭЦП / NCA Node — только строкой в «потенциале развития» README), ML/CatBoost, Laya в рантайме, мультигород, мобильная вёрстка, i18n.

## Чек-лист перед каждым PR

- [ ] `yarn tsc --noEmit` зелёный
- [ ] `yarn test` зелёный
- [ ] `git diff main --stat` не содержит `.env`, `data/*.db`
- [ ] `/code-review` пройден, найденное закрыто или явно оставлено с комментарием
- [ ] коммиты под своим аккаунтом

## Чек-лист перед сдачей

- [ ] README по 11 пунктам, скриншоты, команды проверены на чистом клоне
- [ ] `grep -r "sk-" --exclude-dir=node_modules --exclude-dir=.git .` пусто
- [ ] репозиторий приватный, доступ только команда + организаторы
- [ ] три сида прогнаны, Score совпадает с README
- [ ] «Сдать решение» нажата, название и описание заполнены
