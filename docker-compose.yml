services:
  app:
    build: .
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - .env
    environment:
      DATA_DIR: /app/data/runs
    volumes:
      - runs:/app/data/runs

volumes:
  runs:
