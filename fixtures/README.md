# fixtures

`sample-run.json` — полный `Run` (см. `src/lib/types.ts`) для разработки UI без бэкенда и без ключа OpenAI.
Числа движка посчитаны по формуле датасета для набора M7+M8+M10 (Нура), M12 (город), M5 (Сарыарка): Score 56.54. Улучшение №1 (M5 → ЛРТ в Нуре, 57.21) тоже посчитано по формуле.
Тексты экспертов, черновиков, ревизии и резолюции написаны руками как образец формата.
`optimizer.bestScore` и `percentile` иллюстративные до появления оптимизатора (задача A5).
